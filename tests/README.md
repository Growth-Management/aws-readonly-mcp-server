# Tests

This directory contains the initial test skeleton for the AWS Read Only MCP Server.

## Included tests
- `test_config.py`: basic config loading coverage
- `test_server.py`: minimal server healthcheck coverage

## Recommended next additions
- service layer tests
- AWS client factory tests
- error handling tests
- integration tests with mocked AWS SDK responses
